%p Welcome
=current_user.email if current_user
=current_user.username if current_user
%br

=@search if @search

%ul.topics
  -@topics.each do |t|
    %li
      =t.subject
      =" - "
      -ancestors = t.cliq.ancestors
      -ancestors << t.cliq
      -ancestors.each do |desc|
        =desc.name
