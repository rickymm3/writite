.dropdown.pull-right
  ="by: #{topicreply.user.username}"
  -if current_user.id == topicreply.user_id
    %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
      %i.fa.fa-cog
    %ul.dropdown-menu{role:'menu'}
      %li
        =link_to "Edit", edit_topic_reply_path(@topic, topicreply), class: "btn btn-default"