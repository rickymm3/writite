%h5 Featured Stories
%ul.list-inline
  .row
    -@featured.each do |feature|
      =link_to mystory_path(feature) do
        %li.left.featured-story
          %ul
            %li
              -if feature.image
                =image_tag attachment_url(feature, :image, :fill, 100,140)
              -else
                =image_tag('layout/nocover.png', size: "100x140")
            %li=feature.title
            -#%li
            -#  -feature.tags.each do |tag|
            -#    =tag.name