-if @stories
  .well
    %h3 Your Stories:
    %ul
      -@stories.each do |story|
        %li=link_to story.title, mystory_url(story)
  .well
    =link_to "Create a New Story", new_mystory_path
-else
  =render "shared/must_sign_in"